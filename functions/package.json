{
  "name": "fcoy-functions",
  "description": "FCOY Training-Command ERP \u2014 server-side AI proxy. Holds cloud AI secrets; the browser never sees them.",
  "private": true,
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore,storage,auth",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "npm run test:staff:unit && npm run test:rules && npm run test:storage",
    "test:staff:unit": "node test/staffProvisioning.test.mjs",
    "test:staff:runtime": "mocha --timeout 40000 test/staffProvisioning.runtime.test.mjs",
    "test:rules": "mocha --timeout 20000 test/firestore.rules.test.mjs",
    "test:storage": "mocha --timeout 20000 test/storage.rules.test.mjs",
    "test:rules:emu": "firebase emulators:exec --project demo-fcoy-test --only firestore \"npm run test:rules\"",
    "test:storage:emu": "firebase emulators:exec --project demo-fcoy-test --only storage,firestore \"npm run test:storage\"",
    "test:all": "firebase emulators:exec --project demo-fcoy-test --only firestore,storage \"npm test\""
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.1"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "chai": "^5.1.1",
    "mocha": "^10.7.3"
  }
}
